[build-system]
requires = [
    "setuptools>=42",
    "pybind11>=2.10.0",
    "toml",
]
build-type = "Release"
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["test", "build/cpp-tests",]
norecursedirs = ["_deps"]